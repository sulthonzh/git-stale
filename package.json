{
  "name": "git-stale",
  "version": "1.0.0",
  "description": "Find stale local git branches that are safe to delete",
  "bin": {
    "git-stale": "./cli.js"
  },
  "main": "src/index.js",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "stale": "node cli.js"
  },
  "keywords": ["git", "branches", "stale", "cleanup", "cli"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sulthonzh/git-stale"
  }
}
